<div id="employee-tabs" class="mx-auto">
  <div class="hidden sm:block">
    <div class="border-b border-gray-200">
      <nav class="flex -mb-px space-x-8 lg:px-8" aria-label="Tabs">
        <% if policy(@contact).index? %>
          <a href="<%= profile_timeline_index_path(@contact) %>" class="<%= current_page?(profile_timeline_index_path(@contact)) ? "border-indigo-500 text-indigo-600 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" : "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" %>">
            Timeline
          </a>
        <% end %>
         <% if policy(@contact).index? %>
          <a href="<%= profile_about_index_path(@contact) %>" class="<%= current_page?(profile_about_index_path(@contact)) ? "border-indigo-500 text-indigo-600 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" : "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" %>">
            About
          </a>
        <% end %>
        <% if policy(@contact).index? %>
          <a href="<%= profile_notes_path(@contact) %>" class="<%= current_page?(profile_notes_path(@contact)) ? "border-indigo-500 text-indigo-600 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" : "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" %>">
            Notes
          </a>
        <% end %>
         <% if policy(@contact).index? %>
          <a href="<%= profile_phone_calls_path(@contact) %>" class="<%= current_page?(profile_phone_calls_path(@contact)) ? "border-indigo-500 text-indigo-600 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" : "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" %>">
            Phone Calls
          </a>
        <% end %>
         <% if policy(@contact).index? %>
          <a href="<%= profile_tasks_path(@contact) %>" class="<%= current_page?(profile_tasks_path(@contact)) ? "border-indigo-500 text-indigo-600 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" : "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" %>">
            Tasks
          </a>
        <% end %>
       <% if policy(@contact).index? %>
          <a href="<%= profile_relatives_path(@contact) %>" class="<%= current_page?(profile_relatives_path(@contact)) ? "border-indigo-500 text-indigo-600 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" : "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" %>">
           Relations
          </a>
        <% end %>
      </nav>
    </div>
  </div>
</div>
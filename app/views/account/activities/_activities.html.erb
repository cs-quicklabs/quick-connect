<% activities.each do |group, activity|%>
  <p class="w-full text-lg font-semibold text-gray-900 flex pt-4 pb-2  "><%=group.name%></p>
  <%activity.each do |sub_activity|%>
    <%= render "activity", {activity:sub_activity}%>
  <% end %>
<% end %>

    <main class="pb-10 mx-auto max-w-7xl lg:py-12 lg:px-8">
  <div class="lg:grid lg:grid-cols-12 lg:gap-x-5">
    <%= render "shared/account_menu" %>
    <main class="max-w-xl px-4 pb-12 lg:col-span-8">
      <div class="overflow-hidden bg-white">
        <div class="pb-4 ml-4">
          <%= render "shared/form_header", title: "Untracked contacts", subtitle: @contacts.load.size > 0 ? "Following contacts have been untracked." : "No record found." %>
        </div>
        <div class="overflow-hidden bg-white" data-controller="infinite-scroll">
          <div>
            <table class="table">
              <thead class="bg-gray-50">
              </thead>
              <tbody class="table-body" id="contacts" data-infinite-scroll-target="entries">
                <%= render partial: "contacts/untracked_contact", collection: @contacts, as: :contact, cached: true %>
              </tbody>
            </table>
            <div class="flex justify-end invisible mt-2 mb-6 mr-2" data-infinite-scroll-target="pagination">
              <%== render partial: "shared/paginator", locals: { pagy: @pagy } %>
            </div>
          </div>
        </main>
      </div>
    </main>

<li>
  <div class="relative pb-4">
    <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
    <div class="relative flex space-x-3">
      <div>
        <span class="flex items-center justify-center w-8 h-8 bg-gray-200 rounded-full ring-2 ring-white">
          <%= image_tag "male-placeholder-image.png", class: "w-7 h-7 rounded-full" %>
        </span>
      </div>
      <div class="min-w-0 flex-1 -mt-2 flex justify-between space-x-4">
        <div>
          <p class="text-sm text-gray-500"> <%= display_created_at(event) %></p>
          <div class="mt-1 text-sm text-gray-700">
            <p class="">
              <% if event.account.owner_id == event.user_id %>
                You
              <% else %>
                <%= event.user.decorate.display_name %>
              <% end %>
              <%= event.action_for_context %>
              <% if event.trackable.nil? %>
                which does not exists anymore 
              <% else %>
                <a href="<%= batches_path(:batch_id => event.trackable_id) %>" class="font-medium text-gray-900 hover:underline"><%= event.trackable.name.upcase_first %></a>
              <% end %>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</li>
